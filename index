import React, { useState } from 'react';
import { 
  User, Zap, ChevronDown, LayoutGrid, 
  Settings, Image as ImageIcon, CheckCircle2, 
  Loader2, Play, Search 
} from 'lucide-react';

const ProjectPersona = () => {
  const [isGenerating, setIsGenerating] = useState(false);
  const [progress, setProgress] = useState(85);

  // Mock data for the variations grid
  const variations = Array.from({ length: 40 }).map((_, i) => ({
    id: i,
    label: `Variation #${i + 1}`,
    tag: i % 3 === 0 ? 'Cinematic' : i % 2 === 0 ? 'Office' : 'Street'
  }));

  return (
    <div className="flex flex-col h-screen w-full bg-[#09090b] text-slate-200 font-sans overflow-hidden">
      
      {/* Top Bar */}
      <header className="h-16 border-b border-white/10 px-6 flex items-center justify-between bg-[#0f0f12]">
        <div className="flex items-center gap-3">
          <div className="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center">
            <Zap size={20} className="fill-white text-white" />
          </div>
          <h1 className="text-xl font-bold tracking-tight text-white">Project Persona</h1>
        </div>
        
        <div className="flex items-center gap-6">
          <div className="flex items-center gap-2 px-3 py-1 bg-amber-500/10 border border-amber-500/20 rounded-full">
            <CheckCircle2 size={14} className="text-amber-500" />
            <span className="text-xs font-medium text-amber-500 uppercase tracking-wider">Subscribed</span>
          </div>
          <div className="flex items-center gap-3 border-l border-white/10 pl-6">
            <div className="text-right text-sm">
              <p className="font-medium text-white">Rahul N.</p>
              <p className="text-xs text-slate-500">Professional Plan</p>
            </div>
            <div className="w-10 h-10 rounded-full bg-gradient-to-tr from-blue-500 to-purple-500 border-2 border-[#1a1a1e]" />
          </div>
        </div>
      </header>

      {/* Main Content Layout */}
      <main className="flex flex-1 overflow-hidden">
        
        {/* Left Column: Input Settings */}
        <aside className="w-[400px] border-r border-white/10 bg-[#0f0f12] p-6 overflow-y-auto custom-scrollbar">
          <section className="space-y-6">
            <div>
              <h2 className="text-xs font-bold text-slate-500 uppercase tracking-[0.2em] mb-4">Input Face</h2>
              <div className="relative group rounded-xl overflow-hidden border-2 border-blue-500/50 aspect-square bg-[#1a1a1e]">
                {/* Reference to image_0.png */}
                <img 
                  src="/api/placeholder/400/400" 
                  alt="Source Portrait" 
                  className="w-full h-full object-cover"
                />
                <div className="absolute inset-0 bg-blue-500/10 transition-opacity" />
              </div>
            </div>

            <div className="space-y-4">
              <h2 className="text-xs font-bold text-slate-500 uppercase tracking-[0.2em]">Persona Settings</h2>
              
              {/* Base Model */}
              <div className="space-y-2">
                <label className="text-xs text-slate-400 ml-1">Base Model</label>
                <button className="w-full flex items-center justify-between px-4 py-3 bg-[#1a1a1e] border border-white/5 rounded-lg hover:border-white/20 transition-all">
                  <span className="text-sm">SDXL-Pro-Identity-v2.1</span>
                  <ChevronDown size={16} />
                </button>
              </div>

              {/* Custom Prompt */}
              <div className="space-y-2">
                <label className="text-xs text-slate-400 ml-1">Custom Prompt</label>
                <textarea 
                  className="w-full px-4 py-3 bg-[#1a1a1e] border border-white/5 rounded-lg text-sm h-24 focus:ring-1 focus:ring-blue-500 outline-none resize-none"
                  defaultValue="Modern entrepreneur, clean suit, city view at dusk"
                />
              </div>

              {/* Pose Style */}
              <div className="space-y-2">
                <label className="text-xs text-slate-400 ml-1">Pose Style</label>
                <div className="grid grid-cols-2 gap-2">
                  <button className="p-2 text-xs bg-blue-600/20 border border-blue-500/40 rounded-md text-blue-400">Walking (img_1)</button>
                  <button className="p-2 text-xs bg-[#1a1a1e] border border-white/5 rounded-md hover:bg-[#252529]">Sitting (img_0)</button>
                </div>
              </div>

              {/* Style Presets */}
              <div className="space-y-2">
                <label className="text-xs text-slate-400 ml-1">Style Preset</label>
                <div className="flex flex-wrap gap-2">
                  {['Cinematic', 'Minimalist', 'Anime', 'Vogue'].map(style => (
                    <span key={style} className={`px-3 py-1.5 rounded-full text-xs cursor-pointer border ${style === 'Cinematic' ? 'bg-white text-black border-white' : 'bg-[#1a1a1e] border-white/5 text-slate-400'}`}>
                      {style}
                    </span>
                  ))}
                </div>
              </div>
            </div>

            <button 
              onClick={() => setIsGenerating(!isGenerating)}
              className="w-full py-4 mt-4 bg-blue-600 hover:bg-blue-500 text-white rounded-xl font-bold flex items-center justify-center gap-3 transition-all shadow-lg shadow-blue-900/20"
            >
              {isGenerating ? <Loader2 className="animate-spin" size={20} /> : <Play size={20} className="fill-current" />}
              {isGenerating ? 'GENERATING...' : 'GENERATE'}
            </button>
          </section>
        </aside>

        {/* Right Column: Generated Grid */}
        <section className="flex-1 bg-[#09090b] p-8 overflow-y-auto">
          <div className="flex items-center justify-between mb-8">
            <h2 className="text-xl font-semibold flex items-center gap-3">
              Generated Variations
              <span className="text-xs px-2 py-0.5 bg-white/5 text-slate-500 rounded border border-white/10">120 Total</span>
            </h2>
            <div className="flex gap-4">
              <div className="relative">
                <Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-500" />
                <input className="bg-[#1a1a1e] border border-white/5 rounded-lg pl-10 pr-4 py-2 text-sm outline-none focus:border-blue-500/50 w-64" placeholder="Filter variations..." />
              </div>
              <button className="p-2 bg-[#1a1a1e] border border-white/5 rounded-lg"><LayoutGrid size={20} /></button>
            </div>
          </div>

          <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-6">
            {variations.map((v) => (
              <div key={v.id} className="group relative aspect-[3/4] bg-[#0f0f12] rounded-xl overflow-hidden border border-white/5 hover:border-blue-500/50 transition-all cursor-zoom-in">
                {/* Placeholder for actual LoRA outputs */}
                <div className="w-full h-full bg-gradient-to-b from-slate-800 to-slate-900 flex flex-col justify-end p-4">
                  <div className="w-full h-full absolute inset-0 opacity-40 group-hover:opacity-100 transition-opacity bg-cover bg-center" 
                       style={{ backgroundImage: `url('/api/placeholder/300/400')` }} />
                  <div className="relative z-10">
                    <p className="text-[10px] font-bold text-blue-400 uppercase mb-1">{v.tag}</p>
                    <p className="text-xs text-white font-medium opacity-0 group-hover:opacity-100 transition-opacity tracking-wide">VIEW VARIATION</p>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </section>
      </main>

      {/* Bottom Progress Bar */}
      <footer className="h-12 border-t border-white/10 bg-[#0f0f12] px-6 flex items-center justify-between">
        <div className="flex items-center gap-4 w-1/3">
          <span className="text-[10px] font-bold text-slate-500 uppercase whitespace-nowrap">Progress 85%</span>
          <div className="w-full h-1.5 bg-white/5 rounded-full overflow-hidden">
            <div 
              className="h-full bg-blue-600 rounded-full transition-all duration-500" 
              style={{ width: `${progress}%` }}
            />
          </div>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-2 h-2 bg-green-500 rounded-full animate-pulse" />
          <span className="text-[10px] font-bold text-slate-400 uppercase tracking-widest">Processing 120 variations... Face Swap Active</span>
        </div>
        <div className="w-1/3 flex justify-end gap-4">
           <span className="text-[10px] text-slate-500 uppercase">Latency: 240ms</span>
           <span className="text-[10px] text-slate-500 uppercase">VRAM: 18.2GB / 24GB</span>
        </div>
      </footer>
    </div>
  );
};

export default ProjectPersona;
